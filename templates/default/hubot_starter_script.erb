#!/bin/sh

set -e

<% @env.each do |key, value| %>
export <%= key %>=<%= value %>
<% end %>

npm install
export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

exec node_modules/.bin/hubot --name "<%= @name %>" -a <%= @adapter %>
